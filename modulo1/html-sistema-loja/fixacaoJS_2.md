``` function calculaPrecoTotal(quantidade) {```
```  let preco ```
```  if (quantidade < 12){ ```
```  preco = quantidade * (1 + 3 / 10) ```
```  }; else { ```
```    preco = quantidade * 1 ```
```  }; ```
```  return preco ```
```}; ```